# Manuscript

The IEEE paper is maintained entirely in LaTeX.

`main.tex` is the compilation entry point.

The manuscript will be rewritten only after the implementation audit and controlled experiments are completed. Numerical claims must come from reproducible result files.
